name: my first code
on: workflow_dispatch
jobs:
  first_one:
        runs-on: ubuntu-latest
        steps:
          - name: hello there this is the first code
            run: |
              echo "This is the 1st line"
              echo " This is the 2nd line"
              
  
