name: my first code
on: 
  push:
    branches:
      - main
env:
  APP_ENV: "DEMO1"
jobs:
  env_demo:
        env:
          APP_ENV : "test"
        runs-on: ubuntu-latest
        steps:
          - name: calling .env
            run: echo "please run on the $APP_ENV"
            
  build2:
         runs-on: ubuntu-latest
         steps:
           - name: This is the 2nd line 
             run: echo "2nd line is running perfect"
  deploy:
         runs-on: ubuntu-latest
         steps:
           - name: Using variables in the GHA
             run: echo " Hello this is, ${{vars.My_Name}}"
  deploy1:
          runs-on: ubuntu-latest
          steps:
            - name: checking out the code 
              uses: actions/checkout@v4

            - name: printing the index.html file
              run:  cat index.html

  print_env:
          runs-on: ubuntu-latest
          steps:
            - name: using .env file
              env:
                APP_ENV: "CAT"
              run: echo "App is using $APP_ENV"
    
  
